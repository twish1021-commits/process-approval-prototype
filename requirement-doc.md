# 流程审批模块 - 需求文档

> **原型链接**：顶部切换「手机端 / 云后台」
> **文档版本**：v2.1 | **日期**：2026-07-09
> **阅读对象**：开发工程师、测试工程师、AI 助手

---

## 1. 业务背景

销售套餐（代币销售、门票销售、赠币销售、充值大礼包等）的审批功能在云后台（PC 端）和手机端均已上线。本次为**基于已有功能的迭代优化**，非全新功能开发。

### 1.1 客户痛点

| 客户 | 痛点 | 优化方向 |
|---|---|---|
| 极限主场 | 领导需对多店提交的套餐在手机上审批，数量多容易漏；创建套餐和审批为同一人，逐条处理效率低 | 手机端审批中心与云后台内容对齐；增加批量同意/驳回 |
| 金太阳 | 手机收到审批推送，误以为系统故障；审批详情页内容堆叠、结构不清晰 | 推送按用户维度下发；详情页结构化重构 |

### 1.2 优化目标

| 目标 | 说明 |
|---|---|
| 手机端审批中心对齐 | 三 tab（我的待办/我发起的/我完成的）与云后台一致 |
| 批量审批 | 手机端和云后台「我的待办」支持批量同意/驳回 |
| 详情页结构化 | 字段结构化展示，变更审批支持变更内容与原方案内容对比 |
| 消息入口直达 | 微信公众号消息点击直达审批详情页 |

> 金太阳客户反馈的「审批推送按用户维度下发」属消息推送系统优化，由后端单独处理，不在本文档范围内。本文档仅定义消息点击后的跳转行为（见 §4.6）。

---

## 2. 角色定义

| 角色 | 权限 |
|---|---|
| 发起人 | 提交审批申请，查看「我发起的」列表和审批进度 |
| 审批人 | 处理「我的待办」审批，查看「我完成的」历史记录 |

> 同一用户可同时是发起人和审批人（如极限主场客户：创建套餐和审批为同一人）。

---

## 3. 页面结构

```
流程审批模块
├── 手机端（微信公众号 H5）
│   ├── 消息入口：公众号推送 → 点击直达审批详情页
│   ├── 我的待办（待审批列表，支持批量操作）
│   ├── 我发起的（已提交列表）
│   ├── 我完成的（已审批列表，仅通过记录）
│   └── 审批详情页（字段区 + 审核/意见区 + 流转日志）
│
└── 云后台（PC Web）
    ├── 我的待办（表格 + 批量操作）
    ├── 我发起的（表格）
    ├── 我完成的（表格）
    └── 详情弹窗（提示：详情页面内容与当前云后台版本一致）
```

---

## 4. 手机端详细需求

### 4.1 页面框架

| 元素 | 规则 |
|---|---|
| 导航栏 | 左侧返回按钮（列表页隐藏，详情页显示）+ 返回按钮旁未审批数量 badge + 中间标题 |
| 标签栏 | 三个 tab：我的待办 / 我发起的 / 我完成的 |
| 列表区域 | 卡片式列表，纵向滚动 |
| 底部操作栏 | 仅「我的待办」批量模式下显示：批量同意 / 批量驳回 |

### 4.2 标签页定义

#### 4.2.1 我的待办

| 属性 | 规则 |
|---|---|
| 数据范围 | 当前用户作为审批人、状态为「待审批」的记录 |
| 排序 | 按提交时间倒序 |
| 数量角标 | tab 名称右侧红色角标，显示待审批数量；>99 显示 `99+`；=0 不显示 |
| 列表项 | 左侧色条（橙色）+ 标题 + 业务类型标签 + 提交时间 + 发起人 + 右上角「待审批」标签 |
| 批量操作 | 支持（见 4.4） |

#### 4.2.2 我发起的

| 属性 | 规则 |
|---|---|
| 数据范围 | 当前用户作为发起人提交的、状态为「待审批」的记录 |
| 排序 | 按提交时间倒序 |
| 数量角标 | 不显示 |
| 列表项 | 左侧色条（蓝色）+ 标题 + 业务类型标签 + 提交时间 + 发起人，**不显示**右上角状态 |
| 批量操作 | 不支持 |

#### 4.2.3 我完成的

| 属性 | 规则 |
|---|---|
| 数据范围 | 当前用户作为审批人、**审批通过**的记录（不含拒绝） |
| 排序 | 按提交时间倒序 |
| 数量角标 | 不显示 |
| 列表项 | 左侧色条（绿色）+ 标题 + 业务类型标签 + 提交时间 + 发起人，**不显示**右上角状态、不显示「已同意/已拒绝」 |
| 批量操作 | 不支持 |

### 4.3 审批详情页

#### 4.3.1 页面结构（自上而下）

| 序号 | 区域 | 说明 |
|---|---|---|
| ① | 导航栏 | 返回按钮 + 未审批数量 badge + 标题（审批单标题，超 11 字截断 `…`） |
| ② | 区块标题 | 「待审批内容」（我的待办）/「审批内容」（我发起的、我完成的） |
| ③ | 内容 Tab | 变更审批：双 tab「变更内容 / 原方案内容」；上架审批：无 tab 直接展示 |
| ④ | 字段区 | 结构化展示套餐详情（见 4.3.3） |
| ⑤ | 审核结果 | 仅「我完成的」显示：审批人、审批时间、审批结果、审批备注 |
| ⑥ | 审批意见 | 仅「我的待办」显示：文本输入框（选填） |
| ⑦ | 流转日志 | 所有详情页均显示（见 4.3.4） |
| ⑧ | 底部按钮 | 仅「我的待办」显示：拒绝 / 同意 |

#### 4.3.2 Tab 显示规则

| 审批类型 | Tab 显示 | 变更字段标记 |
|---|---|---|
| 变更审批 | 「变更内容」+「原方案内容」双 tab | 变更内容 tab 中变更字段高亮 |
| 上架审批 | 无 tab，直接展示字段 | 无 |

> 我发起的、我完成的变更审批同样显示双 tab。

#### 4.3.3 字段清单（结构化展示）

| 字段名 | 展示规则 |
|---|---|
| 方案类别 | 文本 |
| 方案名称 | 文本；变更时高亮 |
| 活动编号 | 文本 |
| 活动打折 | 「参与」/「不参与」 |
| 有效期 | `开始日期 ~ 结束日期`；变更时高亮 |
| 授权销售 | 「参与」/「不参与」 |
| 活动详情 | 结构化卡片：① 金额行 ② 赠品行（名称 + `x N` 数量徽章 + 有效期） |
| 活动对象 | 标签气泡：每个会员类型一个气泡；超 4 个折叠显示「+N」 |
| 活动名额 | 「不限制」/ 具体数字 |
| 套餐标签 | `标签名称 · 标签值` 双段标签；无标签显示「—」 |
| 适用门店 | 默认显示「共 N 家」+ 展开图标；点击展开门店列表 |
| 销售渠道 | 渠道 chip + 门店 chip，用 `·` 分隔 |

#### 4.3.4 流转日志

所有详情页底部均显示流转日志时间线。

| 节点 | 阶段 | 操作人 | 时间 | 动作描述 |
|---|---|---|---|---|
| 1 | 申请 | 发起人 | 提交时间 | 单据提交 |
| 2 | 审批 | 审批人 | 审批时间 / `—` | 已通过：「同意」；待审批：「待审批」 |

视觉：已完成节点蓝色实心圆 + 连接线；待审批节点灰色空心圆。

#### 4.3.5 不同 tab 的详情页差异

| 区域 | 我的待办 | 我发起的 | 我完成的 |
|---|---|---|---|
| 区块标题 | 待审批内容 | 审批内容 | 审批内容 |
| 内容 Tab | 变更→双 tab；上架→无 | 同左 | 同左 |
| 审核结果 | 无 | 无 | 显示 |
| 审批意见 | 显示输入框 | 无 | 无 |
| 流转日志 | 显示（待审批节点） | 显示（待审批节点） | 显示（已通过节点） |
| 底部按钮 | 拒绝 / 同意 | 无 | 无 |

### 4.4 批量操作

| 属性 | 规则 |
|---|---|
| 入口 | 「我的待办」列表顶部「批量操作」按钮 |
| 交互流程 | 点击「批量操作」→ 列表项左侧出现复选框 → 勾选审批项 → 底部出现「批量同意 / 批量驳回」 |
| 全选 | 批量模式下顶部出现「全选」+ 已选数量 + 「取消」 |
| 按钮状态 | 未选中任何项时，批量按钮置灰 |
| 确认弹窗 | 点击批量按钮 → 弹窗确认 → 确认/取消 |
| 操作后行为 | 从待办列表移除已处理项 → 移入「我完成的」列表（仅同意）→ 刷新待办数量角标 → Toast 提示 → 返回列表页 |

### 4.5 单条审批操作

| 属性 | 规则 |
|---|---|
| 入口 | 详情页底部「同意 / 拒绝」按钮 |
| 审批意见 | 文本框选填；拒绝时建议填写原因 |
| 操作后行为 | 从待办列表移除 → 移入「我完成的」（仅同意）→ Toast 提示 → 返回列表页 → 刷新角标 |

### 4.6 微信公众号消息入口

| 属性 | 规则 |
|---|---|
| 消息触发 | 有新的待审批任务时，通过微信公众号向审批人推送消息 |
| 点击行为 | 点击消息 → 直接打开该条审批的**详情页**（非列表页） |
| 待审批状态 | 详情页正常显示字段区 + 审批意见输入框 + 底部同意/拒绝按钮，可进行审批 |
| 已审批状态 | 详情页显示审核结果区（审批人/时间/结果/备注）+ 流转日志（已通过节点），**不显示**审批意见输入框和底部按钮 |
| 已被他人处理 | 提示「该审批已被处理」并显示当前审批结果 |

> 消息推送的发送时机、推送频率、消息模板内容由后端消息系统定义，不在本文档范围。

---

## 5. 云后台详细需求

### 5.1 页面框架

| 元素 | 规则 |
|---|---|
| 标签栏 | 三个 tab：我的待办 / 我发起的 / 我完成的（与手机端一致） |
| 列表 | 表格形式 |
| 批量工具栏 | 仅「我的待办」显示：全选复选框 + 已选数量 + 批量驳回 + 批量同意 |

### 5.2 表格列定义

| 列名 | 我的待办 | 我发起的 | 我完成的 |
|---|---|---|---|
| 复选框 | ✅ | ❌ | ❌ |
| 审批标题 | ✅ | ✅ | ✅ |
| 审批类型 | ✅ | ✅ | ✅ |
| 发起人 | ✅ | ✅ | ✅ |
| 提交时间 | ✅ | ✅ | ✅ |
| 操作 | 「详情」 | 「详情」 | 「详情」 |

> 云后台列表不显示状态列。

### 5.3 详情弹窗

| 属性 | 规则 |
|---|---|
| 触发 | 点击「详情」或点击行 |
| 弹窗标题 | 审批单标题 |
| 弹窗内容 | 提示文案：「详情页面内容与当前云后台版本一致」 |
| 底部按钮 | 我的待办：拒绝 / 同意；我发起的 / 我完成的：无 |
| 关闭 | 右上角 × 或遮罩层 |

> 云后台详情弹窗不渲染具体字段，实际详情以云后台现有版本为准。

### 5.4 批量操作

逻辑与手机端一致（见 4.4），区别：表格行勾选代替卡片勾选；表头全选支持全选/反选。

---

## 6. 状态流转

| 当前状态 | 操作 | 目标状态 | 操作人 | 列表变化 |
|---|---|---|---|---|
| — | 提交审批 | 待审批 | 发起人 | 待办 +1，我发起的 +1 |
| 待审批 | 同意（单条/批量） | 已通过 | 审批人 | 待办 -1，我完成的 +1 |
| 待审批 | 拒绝（单条/批量） | 已拒绝 | 审批人 | 待办 -1，我完成的不增加 |

> 已拒绝记录不出现在「我完成的」，发起人可在「我发起的」中看到。

---

## 7. 异常处理

| 场景 | 处理方式 |
|---|---|
| 列表为空 | 显示空状态「暂无记录」 |
| 审批项已被他人处理 | 提示「该审批已被处理」并刷新列表 |
| 网络异常 | Toast 提示「网络异常，请重试」，列表数据不变 |
| 适用门店列表为空 | 显示「共 0 家」，不显示展开列表 |
| 消息点击进入已审批项 | 显示审核结果，不显示审批操作区 |
| 批量操作部分成功/失败 | 提示用户「部分审批处理失败，请重试或联系客服」，刷新列表展示最新状态 |

---

## 8. 验收标准

### 8.1 手机端

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| M-01 | 三 tab 切换 | 切换 tab 正确显示对应列表，数据不串 |
| M-02 | 待办角标 | 数量正确，>99 显示 99+，=0 不显示 |
| M-03 | 返回按钮 badge | 详情页返回按钮旁显示未审批数量 |
| M-04 | 列表排序 | 按提交时间倒序 |
| M-05 | 列表项状态 | 我发起的/我完成的不显示右上角状态标签 |
| M-06 | 我完成列表项 | 显示业务类型标签，不显示「已同意/已拒绝」 |
| M-07 | 变更审批详情 | 显示「变更内容/原方案内容」双 tab，变更字段高亮 |
| M-08 | 上架审批详情 | 无 tab，直接显示字段 |
| M-09 | 我发起的详情 | 变更审批有双 tab，无审核结果区，无审批意见区，有流转日志 |
| M-10 | 我完成的详情 | 变更审批有双 tab，显示审核结果，无审批意见输入框，有流转日志 |
| M-11 | 流转日志 | 所有详情页底部显示时间线 |
| M-12 | 适用门店 | 显示「共 N 家」，点击展开门店列表 |
| M-13 | 活动详情结构化 | 金额行 + 赠品行（名称 + 数量 + 有效期） |
| M-14 | 活动对象 | 标签气泡展示，超 4 个折叠 +N |
| M-15 | 批量操作 | 勾选→底部按钮→确认弹窗→执行→刷新列表+角标 |
| M-16 | 单条审批 | 同意/拒绝后从待办移除，Toast 提示，返回列表 |
| M-17 | 消息入口 | 点击公众号消息直达详情页；待审批可操作，已审批显示结果 |
| M-18 | 分辨率适配 | 320px~768px+ 正常显示，折叠屏展开不变形 |

### 8.2 云后台

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| D-01 | 三 tab 切换 | 与手机端数据一致 |
| D-02 | 表格列 | 无状态列 |
| D-03 | 批量工具栏 | 默认待办 tab 即显示，切换到其他 tab 隐藏 |
| D-04 | 批量操作 | 全选/反选、批量同意/驳回、确认弹窗、执行后刷新 |
| D-05 | 详情弹窗 | 提示「详情页面内容与当前云后台版本一致」 |
| D-06 | 详情弹窗按钮 | 我的待办有同意/拒绝，其他 tab 无 |
| D-07 | 待办角标 | 数量正确，>99 显示 99+ |

### 8.3 多端一致性

| 编号 | 验收项 | 验收标准 |
|---|---|---|
| C-01 | 数据同步 | 手机端审批后，云后台列表实时更新 |
| C-02 | 排序一致 | 手机端和云后台列表排序规则一致 |
| C-03 | 数量一致 | 手机端和云后台待办数量角标一致 |

---

## 9. 分辨率适配

手机端需适配主流手机分辨率，包含折叠屏展开态。

| 分辨率档位 | 典型设备 | 适配要求 |
|---|---|---|
| <360px | iPhone SE | 内容不溢出，可缩小间距/字号 |
| 360~414px | iPhone 12~15、主流 Android | 基准设计尺寸 |
| ≥600px | Galaxy Z Fold 展开 | 放宽内容区宽度，不拉伸变形 |

---

## 10. 术语表

| 术语 | 说明 |
|---|---|
| 上架审批 | 新套餐首次上架需审批 |
| 变更审批 | 已有套餐修改参数后需审批，展示变更对比 |
| 变更字段 | 变更审批中被修改的字段，在「变更内容」tab 中高亮 |
| 流转日志 | 审批全过程的时间线记录 |

---

> 原型中所有数据为示例数据，实际字段以后端接口为准。
